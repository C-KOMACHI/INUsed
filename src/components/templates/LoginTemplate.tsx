import type { FC } from 'react';
import { Input, Button } from '@/components/atoms';

export const LoginTemplate: FC = () => {
    return (
        <>
            <Input />
            <Input />
            <Button />;
        </>
    );
};
